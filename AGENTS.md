<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.

# Next.js Project Rules

This project follows custom architecture and naming conventions.

Before creating, moving, or modifying files, read:

```txt
docs/project-standards.md
```

The rules in that document take precedence over default framework conventions whenever possible.

## Required

- Follow the folder structure defined in `docs/project-standards.md`
- Follow the naming conventions defined in `docs/project-standards.md`
- Use the component structure defined in `docs/project-standards.md`
- Use the type and enum conventions defined in `docs/project-standards.md`
- Prefer colocating code inside the feature route before creating global/shared folders
- Do not introduce new architectural patterns unless explicitly requested

<!-- END:nextjs-agent-rules -->
