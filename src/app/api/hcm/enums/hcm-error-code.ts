export enum HcmErrorCode {
  ValidationError = "validation-error",
  NotFound = "not-found",
  Conflict = "conflict",
  InsufficientBalance = "insufficient-balance",
  HcmUnavailable = "hcm-unavailable",
  ReconciliationNeeded = "reconciliation-needed",
}
